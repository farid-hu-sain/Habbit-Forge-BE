generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum Frequency {
  DAILY
  WEEKLY
  MONTHLY
  YEARLY
}

enum CategoryName {
  HEALTHY
  LEARNING
  FINANCE
  WORK
  SOCIAL
}
