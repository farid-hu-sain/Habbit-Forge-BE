model Category {
  id          String    @id @default(uuid())
  name        CategoryName    @unique
  description String?
  color       String?  
  icon        String? 
  createdAt   DateTime  @default(now())
  habit      Habit[]
  
  @@map("categories")
}